{
  "name": "seestern-fighters",
  "version": "0.15.0",
  "private": true,
  "description": "Seestern Fighters — Browser-Actionspiel (kein Build-Schritt nötig)",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
